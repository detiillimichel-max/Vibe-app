/**
 * OIO ONE - COMMENT SYSTEM 💬
 */
export const CommentSystem = {
    openComments(videoId) {
        // Por enquanto, um alerta de luxo. No futuro, abriremos o banco de dados.
        alert("OIO ONE: Abrindo central de comentários para o vídeo " + videoId);
    }
};
