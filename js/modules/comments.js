/**
 * OIO ONE - COMMENT SYSTEM 💬
 */
export const CommentSystem = {
    openComments(videoId) {
        // Por enquanto, apenas um alerta de luxo para validar a conexão
        alert(`Abrindo comentários do vídeo: ${videoId}`);
        // Futuramente, aqui injetaremos a lista do Supabase
    }
};
