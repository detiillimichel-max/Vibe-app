export const OriginTemplates = {
    // Espaço para Marketing / Patrocinado
    marketingCard: (data) => `
        <div class="glass-card marketing-slot">
            <span class="badge-premium">CURADORIA</span>
            <img src="${data.image}" class="marketing-img">
            <div class="marketing-info">
                <h4>${data.title}</h4>
                <p>${data.description}</p>
                <button class="glass-btn-mini">CONHECER</button>
            </div>
        </div>
    `,

    // Espaço para Sugestão de Conexão
    connectionSuggest: (user) => `
        <div class="suggest-item">
            <div class="avatar-ring">
                <img src="${user.avatar}" alt="${user.name}">
            </div>
            <p>${user.name}</p>
            <button class="btn-add-nexus">+</button>
        </div>
    `
};
